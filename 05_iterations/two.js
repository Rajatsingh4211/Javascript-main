// Do while loop. 

let index = 0

while (index<=10) {
    // console.log(`Value of Index is ${index}`)
    index = index + 2;
}

// DO WHILE LOOP

do {
    
} while (condition); /* This loop execute your code first and then check the condition. But while loop checks the condition first
and then executes the condition */