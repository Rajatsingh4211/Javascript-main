// var subOne = Number (prompt ("Type a subone marks"));
// var subTwo = Number (prompt ("Type a subTwo marks"));

// console.log(((subOne + subTwo)/200)*100);

// var subOne = Number(prompt("Type a subone marks"));
// var subTwo = Number(prompt("Type a subTwo marks"));

// // Avoid using 'let Number' in this context, as it's unnecessary
// // Let's directly calculate and log the percentage
// console.log(((subOne + subTwo) / 200) * 100);
 
/* Note this code is completly fine just the problem is this code don't takes 
input by prompt. But the console in the web browser takes it. So no worries. 
On all these. */ //his is good 

console.log("Hello World");